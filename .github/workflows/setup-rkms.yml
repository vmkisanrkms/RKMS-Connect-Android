name: RKMS Android 16 API 36 - Final Signed Release APK Only

on:
  workflow_dispatch:

permissions:
  contents: read

env:
  ANDROID_API: "36"
  BUILD_TOOLS: "36.0.0"

  NODE_VERSION: "22"
  JAVA_VERSION: "17"

  AGP_VERSION: "8.9.1"
  GRADLE_VERSION: "8.11.1"

  FIREBASE_BOM_VERSION: "34.19.0"
  GOOGLE_SERVICES_VERSION: "4.5.0"
  CREDENTIALS_VERSION: "1.5.0"

  APP_ID: "org.rkms.connect"
  MIN_SDK: "23"
  TARGET_SDK: "36"

  SOURCE_ZIP: "RKMS_CONNECT_FINAL_GROUP_MANAGEMENT_UNRESTRICTED_20260920.zip"

  APK_NAME: "RKMS_CONNECT_ANDROID16_API36_RELEASE.apk"


jobs:

  build:

    name: Build Final Signed Release APK Only

    runs-on: ubuntu-latest

    steps:

      # ============================================================
      # 1. CHECKOUT
      # ============================================================

      - name: Checkout repository
        uses: actions/checkout@v4


      # ============================================================
      # 2. VERIFY EXACT SOURCE ZIP
      # ============================================================

      - name: Verify exact RKMS source ZIP
        shell: bash
        run: |
          set -euo pipefail

          echo "Required source:"
          echo "${SOURCE_ZIP}"

          if [ ! -f "${SOURCE_ZIP}" ]; then
            echo "::error::Required RKMS source ZIP not found."
            echo ""
            echo "Available ZIP files:"
            find . \
              -maxdepth 4 \
              -type f \
              -iname "*.zip" \
              -print \
              | sort \
              || true
            exit 1
          fi

          echo "SOURCE_ZIP=${SOURCE_ZIP}" >> "$GITHUB_ENV"

          echo "Exact source ZIP found:"
          ls -lh "${SOURCE_ZIP}"


      # ============================================================
      # 3. SOURCE ZIP SHA256
      # ============================================================

      - name: Generate source ZIP SHA256
        shell: bash
        run: |
          set -euo pipefail

          sha256sum "${SOURCE_ZIP}" \
            | tee source-zip.sha256

          echo "Source SHA256:"
          cat source-zip.sha256


      # ============================================================
      # 4. EXTRACT
      # ============================================================

      - name: Extract RKMS source
        shell: bash
        run: |
          set -euo pipefail

          rm -rf build-project
          mkdir -p build-project

          unzip -q \
            "${SOURCE_ZIP}" \
            -d build-project

          echo "Source extraction PASS."


      # ============================================================
      # 5. DETECT PROJECT ROOT
      # ============================================================

      - name: Detect RKMS project root
        shell: bash
        run: |
          set -euo pipefail

          ROOT=""

          if [ -f "build-project/package.json" ]; then
            ROOT="build-project"
          else
            ROOT="$(
              find build-project \
                -type f \
                -name "package.json" \
                -not -path "*/node_modules/*" \
                -print \
                | head -n 1 \
                | xargs -r dirname
            )"
          fi

          if [ -z "$ROOT" ]; then
            echo "::error::package.json not found."
            exit 1
          fi

          echo "RKMS_ROOT=$ROOT" >> "$GITHUB_ENV"

          echo "RKMS_ROOT=$ROOT"


      # ============================================================
      # 6. SOURCE VALIDATION
      # ============================================================

      - name: Validate RKMS source
        shell: bash
        run: |
          set -euo pipefail

          cd "$RKMS_ROOT"

          REQUIRED_FILES=(
            "package.json"
            "app.js"
            "capacitor.config.json"
            "google-services.json"
            "scripts/setup-android-native.sh"
          )

          for file in "${REQUIRED_FILES[@]}"; do
            if [ ! -f "$file" ]; then
              echo "::error::Missing required file: $file"
              exit 1
            fi
          done

          echo "Required source files PASS."


      # ============================================================
      # 7. JAVASCRIPT SYNTAX
      # ============================================================

      - name: Verify app.js syntax
        shell: bash
        run: |
          set -euo pipefail

          cd "$RKMS_ROOT"

          node --check app.js

          echo "app.js syntax PASS."


      # ============================================================
      # 8. SHELL SCRIPT SYNTAX
      # ============================================================

      - name: Verify shell scripts
        shell: bash
        run: |
          set -euo pipefail

          cd "$RKMS_ROOT"

          while IFS= read -r -d '' script; do
            echo "Checking: $script"
            bash -n "$script"
          done < <(
            find scripts \
              -type f \
              -name "*.sh" \
              -print0
          )

          echo "Shell script syntax PASS."


      # ============================================================
      # 9. CAPACITOR VERSION
      # ============================================================

      - name: Verify Capacitor versions
        shell: bash
        run: |
          set -euo pipefail

          cd "$RKMS_ROOT"

          node <<'NODE'
          const fs = require("fs");

          const p = JSON.parse(
            fs.readFileSync("package.json", "utf8")
          );

          const deps = {
            ...(p.dependencies || {}),
            ...(p.devDependencies || {})
          };

          const required = {
            "@capacitor/core": "6.2.2",
            "@capacitor/android": "6.2.2",
            "@capacitor/app": "6.0.3",
            "@capacitor/push-notifications": "6.0.3",
            "@capacitor/cli": "6.2.2"
          };

          for (const [name, version] of Object.entries(required)) {
            if (!deps[name]) {
              throw new Error(
                `Missing dependency: ${name}`
              );
            }

            const actual = String(deps[name]);

            if (
              actual !== version &&
              actual !== `^${version}` &&
              actual !== `~${version}`
            ) {
              throw new Error(
                `${name}: expected ${version}, found ${actual}`
              );
            }

            console.log(
              `${name}: ${actual}`
            );
          }

          console.log(
            "Capacitor validation PASS."
          );
          NODE


      # ============================================================
      # 10. NODE 22
      # ============================================================

      - name: Setup Node.js 22
        uses: actions/setup-node@v4
        with:
          node-version: "22"
          cache: npm
          cache-dependency-path: ${{ env.RKMS_ROOT }}/package-lock.json


      # ============================================================
      # 11. NPM INSTALL
      # ============================================================

      - name: Install npm dependencies
        shell: bash
        run: |
          set -euo pipefail

          cd "$RKMS_ROOT"

          if [ -f package-lock.json ]; then
            npm ci \
              --no-audit \
              --no-fund
          else
            npm install \
              --no-audit \
              --no-fund
          fi


      # ============================================================
      # 12. WEB BUILD
      # ============================================================

      - name: Build web application
        shell: bash
        run: |
          set -euo pipefail

          cd "$RKMS_ROOT"

          if node -e '
            const p=require("./package.json");
            process.exit(
              p.scripts && p.scripts.build ? 0 : 1
            );
          '; then
            npm run build
          else
            echo "No build script found."
            echo "Using existing web source."
          fi


      # ============================================================
      # 13. JAVA 17
      # ============================================================

      - name: Setup Java 17
        uses: actions/setup-java@v4
        with:
          distribution: temurin
          java-version: "17"


      # ============================================================
      # 14. ANDROID SDK
      # ============================================================

      - name: Setup Android SDK
        uses: android-actions/setup-android@v4


      # ============================================================
      # 15. INSTALL API 36
      # ============================================================

      - name: Install Android API 36
        shell: bash
        run: |
          set -euo pipefail

          yes | sdkmanager --licenses \
            >/dev/null 2>&1 || true

          sdkmanager \
            "platform-tools" \
            "platforms;android-36" \
            "build-tools;36.0.0"

          echo "Android API 36 installed."


      # ============================================================
      # 16. VERIFY SDK
      # ============================================================

      - name: Verify Android SDK
        shell: bash
        run: |
          set -euo pipefail

          SDK="${ANDROID_SDK_ROOT:-$ANDROID_HOME}"

          test -d \
            "$SDK/platforms/android-36"

          test -d \
            "$SDK/build-tools/36.0.0"

          test -x \
            "$SDK/build-tools/36.0.0/aapt"

          test -x \
            "$SDK/build-tools/36.0.0/apksigner"

          echo "Android API 36 PASS."
          echo "Build Tools 36.0.0 PASS."


      # ============================================================
      # 17. CREATE ANDROID
      # ============================================================

      - name: Create Capacitor Android project
        shell: bash
        run: |
          set -euo pipefail

          cd "$RKMS_ROOT"

          rm -rf android

          npx cap add android


      # ============================================================
      # 18. CAPACITOR SYNC
      # ============================================================

      - name: Capacitor sync Android
        shell: bash
        run: |
          set -euo pipefail

          cd "$RKMS_ROOT"

          npx cap sync android


      # ============================================================
      # 19. ANDROID SDK CONFIG
      # ============================================================

      - name: Configure Android SDK versions
        shell: bash
        run: |
          set -euo pipefail

          cd "$RKMS_ROOT/android"

          python3 <<'PY'
          from pathlib import Path
          import re

          def replace_versions(path):
              if not path.exists():
                  return

              s = path.read_text(
                  encoding="utf-8"
              )

              replacements = [
                  (
                      r'(\bcompileSdkVersion\s*=\s*)\d+',
                      r'\g<1>36'
                  ),
                  (
                      r'(\btargetSdkVersion\s*=\s*)\d+',
                      r'\g<1>36'
                  ),
                  (
                      r'(\bminSdkVersion\s*=\s*)\d+',
                      r'\g<1>23'
                  ),
                  (
                      r'(\bcompileSdk\s*=\s*)\d+',
                      r'\g<1>36'
                  ),
                  (
                      r'(\btargetSdk\s*=\s*)\d+',
                      r'\g<1>36'
                  ),
                  (
                      r'(\bminSdk\s*=\s*)\d+',
                      r'\g<1>23'
                  )
              ]

              for pattern, replacement in replacements:
                  s = re.sub(
                      pattern,
                      replacement,
                      s
                  )

              path.write_text(
                  s,
                  encoding="utf-8"
              )

          replace_versions(
              Path("variables.gradle")
          )

          app = Path(
              "app/build.gradle"
          )

          if not app.exists():
              raise SystemExit(
                  "app/build.gradle missing."
              )

          replace_versions(app)

          print("compileSdk = 36")
          print("targetSdk = 36")
          print("minSdk = 23")
          PY


      # ============================================================
      # 20. AGP 8.9.1
      # ============================================================

      - name: Configure AGP 8.9.1
        shell: bash
        run: |
          set -euo pipefail

          cd "$RKMS_ROOT/android"

          python3 <<'PY'
          from pathlib import Path
          import re

          p = Path(
              "build.gradle"
          )

          s = p.read_text(
              encoding="utf-8"
          )

          s = re.sub(
              r'(com\.android\.tools\.build:gradle:)'
              r'[^"\']+',
              r'\g<1>8.9.1',
              s
          )

          s = re.sub(
              r'(com\.android\.application'
              r'["\']\s+version\s+["\'])'
              r'[^"\']+',
              r'\g<1>8.9.1',
              s
          )

          p.write_text(
              s,
              encoding="utf-8"
          )

          print(
              "AGP 8.9.1 configured."
          )
          PY


      # ============================================================
      # 21. GRADLE 8.11.1
      # ============================================================

      - name: Configure Gradle 8.11.1
        shell: bash
        run: |
          set -euo pipefail

          cd "$RKMS_ROOT/android"

          mkdir -p gradle/wrapper

          python3 <<'PY'
          from pathlib import Path

          p = Path(
              "gradle/wrapper/gradle-wrapper.properties"
          )

          lines = []

          if p.exists():
              lines = p.read_text(
                  encoding="utf-8"
              ).splitlines()

          url = (
              "distributionUrl="
              "https\\://services.gradle.org/"
              "distributions/"
              "gradle-8.11.1-all.zip"
          )

          found = False

          for i, line in enumerate(lines):
              if line.startswith(
                  "distributionUrl="
              ):
                  lines[i] = url
                  found = True

          if not found:
              lines.append(url)

          p.write_text(
              "\n".join(lines) + "\n",
              encoding="utf-8"
          )
          PY

          chmod +x gradlew

          ./gradlew --version


      # ============================================================
      # 22. FIREBASE JSON
      # ============================================================

      - name: Install Firebase google-services.json
        shell: bash
        run: |
          set -euo pipefail

          cd "$RKMS_ROOT"

          test -f google-services.json

          mkdir -p android/app

          cp \
            google-services.json \
            android/app/google-services.json

          test -f \
            android/app/google-services.json


      # ============================================================
      # 23. FIREBASE FCM
      # ============================================================

      - name: Configure Firebase FCM
        shell: bash
        run: |
          set -euo pipefail

          cd "$RKMS_ROOT/android"

          python3 <<'PY'
          from pathlib import Path

          p = Path(
              "app/build.gradle"
          )

          s = p.read_text(
              encoding="utf-8"
          )

          marker = "dependencies {"

          if marker not in s:
              raise SystemExit(
                  "dependencies block missing."
              )

          deps = [
              'implementation platform("com.google.firebase:firebase-bom:34.19.0")',
              'implementation "com.google.firebase:firebase-messaging"'
          ]

          for dep in deps:
              if dep not in s:
                  s = s.replace(
                      marker,
                      marker + "\n    " + dep,
                      1
                  )

          p.write_text(
              s,
              encoding="utf-8"
          )

          print(
              "Firebase dependencies PASS."
          )
          PY


      # ============================================================
      # 24. CREDENTIAL MANAGER
      # ============================================================

      - name: Configure Credential Manager
        shell: bash
        run: |
          set -euo pipefail

          cd "$RKMS_ROOT/android"

          python3 <<'PY'
          from pathlib import Path

          p = Path(
              "app/build.gradle"
          )

          s = p.read_text(
              encoding="utf-8"
          )

          marker = "dependencies {"

          if marker not in s:
              raise SystemExit(
                  "dependencies block missing."
              )

          deps = [
              'implementation "androidx.credentials:credentials:1.5.0"',
              'implementation "androidx.credentials:credentials-play-services-auth:1.5.0"'
          ]

          for dep in deps:
              if dep not in s:
                  s = s.replace(
                      marker,
                      marker + "\n    " + dep,
                      1
                  )

          p.write_text(
              s,
              encoding="utf-8"
          )

          print(
              "Credential Manager PASS."
          )
          PY


      # ============================================================
      # 25. GOOGLE SERVICES PLUGIN
      # ============================================================

      - name: Configure Google Services plugin
        shell: bash
        run: |
          set -euo pipefail

          cd "$RKMS_ROOT/android"

          python3 <<'PY'
          from pathlib import Path
          import re

          root = Path(
              "build.gradle"
          )

          app = Path(
              "app/build.gradle"
          )

          root_text = root.read_text(
              encoding="utf-8"
          )

          m = re.search(
              r'buildscript\s*\{',
              root_text
          )

          if not m:
              raise SystemExit(
                  "buildscript block missing."
              )

          start = m.start()

          depth = 0
          end = None

          for i in range(
              start,
              len(root_text)
          ):
              if root_text[i] == "{":
                  depth += 1

              elif root_text[i] == "}":
                  depth -= 1

                  if depth == 0:
                      end = i + 1
                      break

          if end is None:
              raise SystemExit(
                  "Cannot parse buildscript."
              )

          buildscript = root_text[
              start:end
          ]

          if "google()" not in buildscript:

              repo = re.search(
                  r'repositories\s*\{',
                  buildscript
              )

              if not repo:
                  raise SystemExit(
                      "buildscript repositories missing."
                  )

              pos = repo.end()

              buildscript = (
                  buildscript[:pos]
                  + "\n        google()"
                  + buildscript[pos:]
              )

          classpath = (
              "com.google.gms:"
              "google-services:4.5.0"
          )

          if classpath not in buildscript:

              deps = re.search(
                  r'dependencies\s*\{',
                  buildscript
              )

              if not deps:
                  raise SystemExit(
                      "buildscript dependencies missing."
                  )

              pos = deps.end()

              buildscript = (
                  buildscript[:pos]
                  + "\n        classpath "
                  "'com.google.gms:"
                  "google-services:4.5.0'"
                  + buildscript[pos:]
              )

          root_text = (
              root_text[:start]
              + buildscript
              + root_text[end:]
          )

          root.write_text(
              root_text,
              encoding="utf-8"
          )

          app_text = app.read_text(
              encoding="utf-8"
          )

          plugin = \
              "com.google.gms.google-services"

          if plugin not in app_text:

              if "plugins {" in app_text:

                  app_text = app_text.replace(
                      "plugins {",
                      "plugins {\n"
                      "    id "
                      "'com.google.gms.google-services'",
                      1
                  )

              else:

                  app_text = (
                      "apply plugin: "
                      "'com.google.gms.google-services'"
                      "\n\n"
                      + app_text
                  )

          app.write_text(
              app_text,
              encoding="utf-8"
          )

          print(
              "Google Services plugin PASS."
          )
          PY


      # ============================================================
      # 26. RKMS NATIVE SETUP SCRIPT
      # ============================================================

      - name: Prepare RKMS native setup script
        shell: bash
        run: |
          set -euo pipefail

          cd "$RKMS_ROOT"

          chmod +x \
            scripts/setup-android-native.sh

          python3 <<'PY'
          from pathlib import Path

          p = Path(
              "scripts/setup-android-native.sh"
          )

          s = p.read_text(
              encoding="utf-8"
          )

          # --------------------------------------------------------
          # IMPORTANT FIX:
          #
          # The source ZIP contains a Credential Manager callback
          # where Java newlines were accidentally written as literal
          # "\\n" characters.
          #
          # Fix only that generated Java callback.
          # Do NOT replace all "\\n" in the shell script because
          # many of those are legitimate Python string escapes.
          # --------------------------------------------------------

          old = (
              '@Override public void onResult('
              'androidx.credentials.CreateCredentialResponse result){'
              '\\\\n        String js='
          )

          new = (
              '@Override public void onResult('
              'androidx.credentials.CreateCredentialResponse result){'
              '\n        String js='
          )

          if old in s:
              s = s.replace(
                  old,
                  new
              )

          old2 = (
              'org.json.JSONObject.quote(identifier.trim())+"}}));";\\\\n'
              '        webView.post'
          )

          new2 = (
              'org.json.JSONObject.quote(identifier.trim())+"}}));";'
              '\n        webView.post'
          )

          if old2 in s:
              s = s.replace(
                  old2,
                  new2
              )

          old3 = (
              'webView.evaluateJavascript(js,null));\\\\n'
              '      }'
          )

          new3 = (
              'webView.evaluateJavascript(js,null));'
              '\n      }'
          )

          if old3 in s:
              s = s.replace(
                  old3,
                  new3
              )

          p.write_text(
              s,
              encoding="utf-8"
          )

          print(
              "Native setup source prepared."
          )
          PY

          bash -n \
            scripts/setup-android-native.sh

          echo "Native setup script syntax PASS."


      # ============================================================
      # 27. RUN RKMS NATIVE SETUP
      # ============================================================

      - name: Configure RKMS native Android bridge
        shell: bash
        run: |
          set -euo pipefail

          cd "$RKMS_ROOT"

          ./scripts/setup-android-native.sh

          echo "RKMS native Android setup PASS."


      # ============================================================
      # 28. FINAL FIX GENERATED RKMSNativeBridge.java
      # ============================================================

      - name: Fix generated RKMSNativeBridge Java source
        shell: bash
        run: |
          set -euo pipefail

          cd "$RKMS_ROOT"

          BRIDGE="$(
            find android/app/src/main/java \
              -type f \
              -name "RKMSNativeBridge.java" \
              -print \
              | head -n 1
          )"

          if [ -z "$BRIDGE" ]; then
            echo "::error::RKMSNativeBridge.java not found."
            exit 1
          fi

          python3 - "$BRIDGE" <<'PY'
          from pathlib import Path
          import sys

          p = Path(
              sys.argv[1]
          )

          s = p.read_text(
              encoding="utf-8"
          )

          # Convert ONLY the malformed Credential Manager
          # callback newlines from literal \n to real newlines.

          s = s.replace(
              'result){\\n        String js=',
              'result){\n        String js='
          )

          s = s.replace(
              '}}));";\\n        webView.post',
              '}}));";\n        webView.post'
          )

          s = s.replace(
              'evaluateJavascript(js,null));\\n      }',
              'evaluateJavascript(js,null));\n      }'
          )

          p.write_text(
              s,
              encoding="utf-8"
          )

          s = p.read_text(
              encoding="utf-8"
          )

          # The exact malformed pattern must no longer exist.
          if (
              'result){\\n        String js='
              in s
          ):
              raise SystemExit(
                  "Literal \\\\n remains in "
                  "Credential Manager callback."
              )

          if (
              'evaluateJavascript(js,null));\\n'
              in s
          ):
              raise SystemExit(
                  "Literal \\\\n remains in "
                  "evaluateJavascript callback."
              )

          print(
              "Generated RKMSNativeBridge.java "
              "newline fix PASS."
          )
          PY

          echo "Bridge:"
          echo "$BRIDGE"

          grep -n \
            "CreateCredentialResponse" \
            "$BRIDGE"

          grep -n \
            "password-saved" \
            "$BRIDGE"


      # ============================================================
      # 29. VERIFY CREDENTIAL MANAGER
      # ============================================================

      - name: Verify Credential Manager
        shell: bash
        run: |
          set -euo pipefail

          cd "$RKMS_ROOT"

          BRIDGE="$(
            find android/app/src/main/java \
              -type f \
              -name "RKMSNativeBridge.java" \
              -print \
              | head -n 1
          )"

          test -n "$BRIDGE"

          grep -Fq \
            "CredentialManager" \
            "$BRIDGE"

          grep -Fq \
            "CreatePasswordRequest" \
            "$BRIDGE"

          grep -Fq \
            "savePassword" \
            "$BRIDGE"

          grep -Fq \
            "createCredentialAsync" \
            "$BRIDGE"

          python3 - "$BRIDGE" <<'PY'
          from pathlib import Path
          import re
          import sys

          s = Path(
              sys.argv[1]
          ).read_text(
              encoding="utf-8"
          )

          if not re.search(
              r'createCredentialAsync\s*\(\s*'
              r'activity\s*,\s*'
              r'req\s*,',
              s,
              re.S
          ):
              raise SystemExit(
                  "Wrong Credential Manager argument order."
              )

          if r'\n        String js=' in s:
              raise SystemExit(
                  "Literal Java newline escape found."
              )

          print(
              "Credential Manager verification PASS."
          )
          PY


      # ============================================================
      # 30. OLD PASSWORD VAULT ABSENT
      # ============================================================

      - name: Verify old password vault removed
        shell: bash
        run: |
          set -euo pipefail

          cd "$RKMS_ROOT"

          if grep -nE \
            'RKMSSecureCredentialsPlugin|RKMSSecureCredentials' \
            scripts/setup-android-native.sh; then

            echo "::error::Old custom password vault found."
            exit 1
          fi

          echo "Old password vault absent."


      # ============================================================
      # 31. MICROPHONE + FILE PICKER
      # ============================================================

      - name: Verify microphone and file picker
        shell: bash
        run: |
          set -euo pipefail

          cd "$RKMS_ROOT"

          grep -Fq \
            "RECORD_AUDIO" \
            scripts/setup-android-native.sh

          grep -Fq \
            "RESOURCE_AUDIO_CAPTURE" \
            scripts/setup-android-native.sh

          grep -Fq \
            "onShowFileChooser" \
            scripts/setup-android-native.sh

          grep -Fq \
            "MediaRecorder" \
            app.js

          echo "Microphone + file picker PASS."


      # ============================================================
      # 32. KEYBOARD RESIZE
      # ============================================================

      - name: Verify keyboard resize
        shell: bash
        run: |
          set -euo pipefail

          cd "$RKMS_ROOT"

          grep -Fq \
            "SOFT_INPUT_ADJUST_RESIZE" \
            scripts/setup-android-native.sh

          echo "Keyboard resize PASS."


      # ============================================================
      # 33. FIREBASE INITIALIZATION
      # ============================================================

      - name: Verify FirebaseApp initialization
        shell: bash
        run: |
          set -euo pipefail

          cd "$RKMS_ROOT"

          MAIN_ACTIVITY="$(
            find android/app/src/main/java \
              -type f \
              -name "MainActivity.java" \
              -print \
              | head -n 1
          )"

          test -n "$MAIN_ACTIVITY"

          grep -Fq \
            "FirebaseApp.initializeApp" \
            "$MAIN_ACTIVITY"

          echo "FirebaseApp initialization PASS."


      # ============================================================
      # 34. FIREBASE CONFIGURATION
      # ============================================================

      - name: Verify Firebase configuration
        shell: bash
        run: |
          set -euo pipefail

          cd "$RKMS_ROOT/android"

          test -f \
            app/google-services.json

          grep -Fq \
            "firebase-bom:34.19.0" \
            app/build.gradle

          grep -Fq \
            "firebase-messaging" \
            app/build.gradle

          grep -Fq \
            "com.google.gms.google-services" \
            app/build.gradle

          grep -Fq \
            "com.google.gms:google-services:4.5.0" \
            build.gradle

          echo "Firebase configuration PASS."


      # ============================================================
      # 35. FIREBASE PACKAGE
      # ============================================================

      - name: Verify Firebase package
        shell: bash
        run: |
          set -euo pipefail

          cd "$RKMS_ROOT/android"

          python3 <<'PY'
          import json

          with open(
              "app/google-services.json",
              encoding="utf-8"
          ) as f:
              data = json.load(f)

          found = False

          for client in data.get(
              "client",
              []
          ):

              package_name = (
                  client
                  .get(
                      "client_info",
                      {}
                  )
                  .get(
                      "android_client_info",
                      {}
                  )
                  .get(
                      "package_name"
                  )
              )

              if package_name == \
                  "org.rkms.connect":

                  found = True
                  break

          if not found:
              raise SystemExit(
                  "Firebase package mismatch."
              )

          print(
              "Firebase package PASS."
          )
          PY


      # ============================================================
      # 36. APPLICATION ID
      # ============================================================

      - name: Verify application ID
        shell: bash
        run: |
          set -euo pipefail

          cd "$RKMS_ROOT/android"

          grep -Rqs \
            "org.rkms.connect" \
            app/build.gradle \
            app/src/main/AndroidManifest.xml

          echo "Application ID PASS."


      # ============================================================
      # 37. FINAL ANDROID CONFIG
      # ============================================================

      - name: Final Android configuration audit
        shell: bash
        run: |
          set -euo pipefail

          cd "$RKMS_ROOT/android"

          echo "=============================================="
          echo "FINAL ANDROID CONFIGURATION"
          echo "=============================================="
          echo "compileSdk : 36"
          echo "targetSdk  : 36"
          echo "minSdk     : 23"
          echo "AGP        : 8.9.1"
          echo "Gradle     : 8.11.1"
          echo "Java       : 17"
          echo "Firebase   : 34.19.0"
          echo "FCM        : firebase-messaging"
          echo "Credential : 1.5.0"
          echo "Package    : org.rkms.connect"
          echo "APK ONLY   : YES"
          echo "=============================================="

          if grep -RniE \
            'minSdkVersion\s+22|minSdkVersion\s*=\s*22|minSdk\s+22|minSdk\s*=\s*22' \
            app/build.gradle \
            variables.gradle \
            2>/dev/null; then

            echo "::error::minSdk 22 still exists."
            exit 1
          fi

          echo "minSdk 22 not found."


      # ============================================================
      # 38. RELEASE SIGNING KEYSTORE
      # ============================================================

      - name: Prepare release signing keystore
        shell: bash
        env:
          RKMS_KEYSTORE_BASE64: ${{ secrets.RKMS_KEYSTORE_BASE64 }}
          RKMS_KEY_ALIAS: ${{ secrets.RKMS_KEY_ALIAS }}
          RKMS_KEYSTORE_PASSWORD: ${{ secrets.RKMS_KEYSTORE_PASSWORD }}
          RKMS_KEY_PASSWORD: ${{ secrets.RKMS_KEY_PASSWORD }}
        run: |
          set -euo pipefail

          if [ -z "${RKMS_KEYSTORE_BASE64:-}" ]; then
            echo "::error::RKMS_KEYSTORE_BASE64 secret is missing."
            exit 1
          fi

          if [ -z "${RKMS_KEY_ALIAS:-}" ]; then
            echo "::error::RKMS_KEY_ALIAS secret is missing."
            exit 1
          fi

          if [ -z "${RKMS_KEYSTORE_PASSWORD:-}" ]; then
            echo "::error::RKMS_KEYSTORE_PASSWORD secret is missing."
            exit 1
          fi

          if [ -z "${RKMS_KEY_PASSWORD:-}" ]; then
            echo "::error::RKMS_KEY_PASSWORD secret is missing."
            exit 1
          fi

          cd "$RKMS_ROOT/android"

          echo "$RKMS_KEYSTORE_BASE64" \
            | base64 --decode \
            > rkms-release.keystore

          test -s \
            rkms-release.keystore

          echo "RKMS_KEYSTORE_PATH=$PWD/rkms-release.keystore" \
            >> "$GITHUB_ENV"

          echo "Release keystore prepared."


      # ============================================================
      # 39. RELEASE SIGNING CONFIG
      # ============================================================

      - name: Configure release signing
        shell: bash
        run: |
          set -euo pipefail

          cd "$RKMS_ROOT/android"

          python3 <<'PY'
          from pathlib import Path

          p = Path(
              "app/build.gradle"
          )

          s = p.read_text(
              encoding="utf-8"
          )

          marker = "android {"

          if marker not in s:
              raise SystemExit(
                  "android block not found."
              )

          if "RKMS_KEYSTORE_PATH" not in s:

              signing_block = r'''
          signingConfigs {
              release {
                  storeFile file(
                      System.getenv("RKMS_KEYSTORE_PATH")
                  )
                  storePassword System.getenv(
                      "RKMS_KEYSTORE_PASSWORD"
                  )
                  keyAlias System.getenv(
                      "RKMS_KEY_ALIAS"
                  )
                  keyPassword System.getenv(
                      "RKMS_KEY_PASSWORD"
                  )
              }
          }

          '''

              s = s.replace(
                  marker,
                  marker
                  + "\n"
                  + signing_block,
                  1
              )

          if (
              "signingConfig signingConfigs.release"
              not in s
          ):

              buildtypes_pos = s.find(
                  "buildTypes"
              )

              if buildtypes_pos < 0:
                  raise SystemExit(
                      "buildTypes block not found."
                  )

              release_pos = s.find(
                  "release {",
                  buildtypes_pos
              )

              if release_pos < 0:
                  raise SystemExit(
                      "release buildType not found."
                  )

              insert_pos = (
                  release_pos
                  + len("release {")
              )

              s = (
                  s[:insert_pos]
                  + "\n"
                  "        signingConfig "
                  "signingConfigs.release"
                  + s[insert_pos:]
              )

          p.write_text(
              s,
              encoding="utf-8"
          )

          print(
              "Release signing configured."
          )
          PY


      # ============================================================
      # 40. VERIFY SIGNING
      # ============================================================

      - name: Verify release signing configuration
        shell: bash
        run: |
          set -euo pipefail

          cd "$RKMS_ROOT/android"

          grep -Fq \
            "signingConfigs {" \
            app/build.gradle

          grep -Fq \
            "signingConfigs.release" \
            app/build.gradle

          grep -Fq \
            "RKMS_KEYSTORE_PATH" \
            app/build.gradle

          test -f \
            "$RKMS_KEYSTORE_PATH"

          echo "Release signing configuration PASS."


      # ============================================================
      # 41. CLEAN
      # ============================================================

      - name: Clean Android project
        shell: bash
        run: |
          set -euo pipefail

          cd "$RKMS_ROOT/android"

          ./gradlew clean \
            --no-daemon


      # ============================================================
      # 42. BUILD FINAL RELEASE APK ONLY
      # ============================================================

      - name: Build Final Signed Release APK
        shell: bash
        env:
          RKMS_KEYSTORE_PATH: ${{ env.RKMS_KEYSTORE_PATH }}
          RKMS_KEYSTORE_PASSWORD: ${{ secrets.RKMS_KEYSTORE_PASSWORD }}
          RKMS_KEY_ALIAS: ${{ secrets.RKMS_KEY_ALIAS }}
          RKMS_KEY_PASSWORD: ${{ secrets.RKMS_KEY_PASSWORD }}
        run: |
          set -euo pipefail

          cd "$RKMS_ROOT/android"

          echo "=============================================="
          echo "RKMS FINAL SIGNED RELEASE APK"
          echo "=============================================="
          echo "Source ZIP       : ${SOURCE_ZIP}"
          echo "Android API      : 36"
          echo "Build Tools      : 36.0.0"
          echo "minSdk           : 23"
          echo "targetSdk        : 36"
          echo "Node             : 22"
          echo "Java             : 17"
          echo "AGP              : 8.9.1"
          echo "Gradle           : 8.11.1"
          echo "Firebase BOM     : 34.19.0"
          echo "Google Services  : 4.5.0"
          echo "Credential Mgr   : 1.5.0"
          echo "Package          : org.rkms.connect"
          echo "SIGNED           : YES"
          echo "APK ONLY         : YES"
          echo "AAB              : NO"
          echo "DEBUG APK        : NO"
          echo "=============================================="

          ./gradlew \
            :app:assembleRelease \
            --no-daemon \
            --stacktrace


      # ============================================================
      # 43. LOCATE APK
      # ============================================================

      - name: Locate release APK
        shell: bash
        run: |
          set -euo pipefail

          cd "$RKMS_ROOT/android"

          APK="$(
            find \
              app/build/outputs/apk/release \
              -type f \
              -name "*.apk" \
              ! -iname "*debug*" \
              -print \
              | head -n 1
          )"

          if [ -z "$APK" ]; then
            echo "::error::Release APK not found."

            find \
              app/build/outputs \
              -type f \
              -print \
              2>/dev/null \
              || true

            exit 1
          fi

          cp \
            "$APK" \
            "$GITHUB_WORKSPACE/${APK_NAME}"

          echo \
            "APK_PATH=$GITHUB_WORKSPACE/${APK_NAME}" \
            >> "$GITHUB_ENV"

          echo "APK created:"
          ls -lh \
            "$GITHUB_WORKSPACE/${APK_NAME}"


      # ============================================================
      # 44. VERIFY APK PACKAGE + SDK
      # ============================================================

      - name: Verify APK package and SDK
        shell: bash
        run: |
          set -euo pipefail

          APK="$APK_PATH"

          "$ANDROID_HOME/build-tools/${BUILD_TOOLS}/aapt" \
            dump badging \
            "$APK" \
            > "$GITHUB_WORKSPACE/apk-badging.txt"

          cat \
            "$GITHUB_WORKSPACE/apk-badging.txt"

          grep -Fq \
            "package: name='${APP_ID}'" \
            "$GITHUB_WORKSPACE/apk-badging.txt"

          grep -Fq \
            "targetSdkVersion:'36'" \
            "$GITHUB_WORKSPACE/apk-badging.txt"

          echo "APK package and SDK PASS."


      # ============================================================
      # 45. VERIFY SIGNATURE
      # ============================================================

      - name: Verify APK signature
        shell: bash
        run: |
          set -euo pipefail

          APK="$APK_PATH"

          "$ANDROID_HOME/build-tools/${BUILD_TOOLS}/apksigner" \
            verify \
            --verbose \
            "$APK"

          echo "APK SIGNATURE PASS."


      # ============================================================
      # 46. VERIFY SIGNER CERTIFICATE
      # ============================================================

      - name: Verify APK signer certificate
        shell: bash
        run: |
          set -euo pipefail

          APK="$APK_PATH"

          "$ANDROID_HOME/build-tools/${BUILD_TOOLS}/apksigner" \
            verify \
            --print-certs \
            "$APK"

          echo "Signer certificate verified."


      # ============================================================
      # 47. VERIFY RELEASE APK
      # ============================================================

      - name: Verify APK is release
        shell: bash
        run: |
          set -euo pipefail

          APK="$APK_PATH"

          BADGING="$(
            "$ANDROID_HOME/build-tools/${BUILD_TOOLS}/aapt" \
              dump badging \
              "$APK"
          )"

          if echo "$BADGING" | grep -qi "debug"; then
            echo "::error::Debug APK detected."
            exit 1
          fi

          echo "Release APK verification PASS."


      # ============================================================
      # 48. SHA256
      # ============================================================

      - name: Generate APK SHA256
        shell: bash
        run: |
          set -euo pipefail

          cd "$GITHUB_WORKSPACE"

          sha256sum \
            "$APK_NAME" \
            | tee \
              "${APK_NAME}.sha256"


      # ============================================================
      # 49. FINAL APK ONLY AUDIT
      # ============================================================

      - name: Final APK-only audit
        shell: bash
        run: |
          set -euo pipefail

          cd "$GITHUB_WORKSPACE"

          echo "=============================================="
          echo "FINAL ARTIFACT AUDIT"
          echo "=============================================="

          echo "APK files:"
          find . \
            -maxdepth 1 \
            -type f \
            -name "*.apk" \
            -printf "%f\n" \
            | sort

          echo ""
          echo "AAB files:"
          find . \
            -maxdepth 1 \
            -type f \
            -name "*.aab" \
            -printf "%f\n" \
            | sort \
            || true

          echo ""
          echo "SHA256 files:"
          find . \
            -maxdepth 1 \
            -type f \
            -name "*.sha256" \
            -printf "%f\n" \
            | sort

          if find . \
            -maxdepth 1 \
            -type f \
            -name "*.aab" \
            | grep -q .; then

            echo "::error::AAB detected."
            exit 1
          fi

          if find . \
            -maxdepth 1 \
            -type f \
            -iname "*debug*.apk" \
            | grep -q .; then

            echo "::error::Debug APK detected."
            exit 1
          fi

          test -f \
            "$APK_NAME"

          test -f \
            "${APK_NAME}.sha256"

          SIZE_BYTES=$(
            stat -c%s "$APK_NAME"
          )

          if [ "$SIZE_BYTES" -le 1000000 ]; then
            echo "::error::APK size is unexpectedly small."
            exit 1
          fi

          echo ""
          echo "=============================================="
          echo "FINAL AUDIT PASSED"
          echo "=============================================="
          echo "APK ONLY       : YES"
          echo "AAB            : NO"
          echo "DEBUG APK      : NO"
          echo "SIGNED         : YES"
          echo "API            : 36"
          echo "MIN SDK        : 23"
          echo "TARGET SDK     : 36"
          echo "PACKAGE        : ${APP_ID}"
          echo "APK            : ${APK_NAME}"
          echo "SIZE BYTES     : ${SIZE_BYTES}"
          echo "=============================================="


      # ============================================================
      # 50. UPLOAD FINAL APK + SHA256
      # ============================================================

      - name: Upload final APK and SHA256
        uses: actions/upload-artifact@v4
        with:
          name: RKMS_ANDROID16_API36_SIGNED_RELEASE_APK
          if-no-files-found: error
          path: |
            RKMS_CONNECT_ANDROID16_API36_RELEASE.apk
            RKMS_CONNECT_ANDROID16_API36_RELEASE.apk.sha256
          retention-days: 30
