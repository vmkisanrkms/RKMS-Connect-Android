name: RKMS Final Signed Release APK

on:
  workflow_dispatch:

permissions:
  contents: read

env:
  ANDROID_API: "36"
  BUILD_TOOLS: "36.0.0"
  NODE_VERSION: "22"
  JAVA_VERSION: "17"
  AGP_VERSION: "8.9.1"
  GRADLE_VERSION: "8.11.1"
  APP_ID: "org.rkms.connect"
  SOURCE_ZIP: "RKMS_CONNECT_NOTIFICATION_SYSTEM_FULL_FIX_20260923_AUDITED_FIXED.zip"

jobs:
  build-release-apk:
    name: Build Signed Release APK
    runs-on: ubuntu-24.04

    steps:

      # ============================================================
      # 1. CHECKOUT
      # ============================================================
      - name: Checkout repository
        uses: actions/checkout@v4

      # ============================================================
      # 2. JAVA 17
      # ============================================================
      - name: Setup Java 17
        uses: actions/setup-java@v5
        with:
          distribution: temurin
          java-version: "17"

      - name: Verify Java
        shell: bash
        run: |
          set -euo pipefail
          java -version
          javac -version

      # ============================================================
      # 3. NODE 22
      # ============================================================
      - name: Setup Node.js 22
        uses: actions/setup-node@v4
        with:
          node-version: "22"

      - name: Verify Node and npm
        shell: bash
        run: |
          set -euo pipefail
          node --version
          npm --version

      # ============================================================
      # 4. ANDROID SDK
      # ============================================================
      - name: Setup Android SDK
        uses: android-actions/setup-android@v4
        with:
          packages: "platform-tools"

      - name: Install Android API 36 and Build Tools
        shell: bash
        run: |
          set -euo pipefail

          export ANDROID_SDK_ROOT="${ANDROID_SDK_ROOT:-$ANDROID_HOME}"

          echo "ANDROID_HOME=$ANDROID_HOME"
          echo "ANDROID_SDK_ROOT=$ANDROID_SDK_ROOT"

          yes | sdkmanager --licenses >/dev/null 2>&1 || true

          sdkmanager \
            "platform-tools" \
            "platforms;android-36" \
            "build-tools;36.0.0"

          yes | sdkmanager --licenses >/dev/null 2>&1 || true

          echo "Android SDK packages installed successfully."

      - name: Verify Android SDK tools
        shell: bash
        run: |
          set -euo pipefail

          command -v adb
          command -v sdkmanager

          test -x "$ANDROID_SDK_ROOT/build-tools/36.0.0/aapt"
          test -x "$ANDROID_SDK_ROOT/build-tools/36.0.0/apksigner"

          "$ANDROID_SDK_ROOT/build-tools/36.0.0/aapt" version
          "$ANDROID_SDK_ROOT/build-tools/36.0.0/apksigner" version
          adb version
          sdkmanager --version

      # ============================================================
      # 5. LOCATE SOURCE ZIP
      # ============================================================
      - name: Locate RKMS source ZIP
        shell: bash
        run: |
          set -euo pipefail

          echo "Looking for:"
          echo "$SOURCE_ZIP"

          if [ ! -f "$SOURCE_ZIP" ]; then
            echo "Exact ZIP not found at repository root."
            echo "Searching repository..."

            FOUND_ZIP="$(find . -type f -name "$SOURCE_ZIP" -print -quit || true)"

            if [ -z "$FOUND_ZIP" ]; then
              echo "ERROR: Source ZIP not found."
              echo "Available ZIP files:"
              find . -type f -iname "*.zip" -print || true
              exit 1
            fi

            cp "$FOUND_ZIP" "./$SOURCE_ZIP"
          fi

          test -f "$SOURCE_ZIP"

          echo "SOURCE ZIP FOUND:"
          ls -lh "$SOURCE_ZIP"

      # ============================================================
      # 6. EXTRACT SOURCE
      # ============================================================
      - name: Extract RKMS source
        shell: bash
        run: |
          set -euo pipefail

          rm -rf source extracted
          mkdir -p source extracted

          unzip -q "$SOURCE_ZIP" -d extracted

          PACKAGE_JSON="$(find extracted -type f -name package.json -print -quit || true)"

          if [ -z "$PACKAGE_JSON" ]; then
            echo "ERROR: package.json not found inside ZIP."
            exit 1
          fi

          SOURCE_ROOT="$(dirname "$PACKAGE_JSON")"

          echo "SOURCE_ROOT=$SOURCE_ROOT"

          cp -a "$SOURCE_ROOT"/. source/

          echo "Source copied."

      # ============================================================
      # 7. VERIFY REQUIRED FILES
      # ============================================================
      - name: Verify required source files
        shell: bash
        run: |
          set -euo pipefail

          cd source

          REQUIRED_FILES=(
            "package.json"
            "package-lock.json"
            "app.js"
            "index.html"
            "styles.css"
            "capacitor.config.json"
            "google-services.json"
            "scripts/setup-android-native.sh"
            "scripts/configure-android-16.sh"
            "scripts/configure-release-signing.sh"
          )

          for file in "${REQUIRED_FILES[@]}"; do
            test -f "$file" || {
              echo "ERROR: Missing required file: $file"
              exit 1
            }

            echo "FOUND: $file"
          done

          echo "Required source files: PASS"

      # ============================================================
      # 8. VERIFY CAPACITOR VERSIONS
      # ============================================================
      - name: Verify Capacitor versions
        shell: bash
        run: |
          set -euo pipefail

          cd source

          node <<'NODE'
          const fs = require('fs');

          const pkg = JSON.parse(
            fs.readFileSync('package.json', 'utf8')
          );

          const expected = {
            '@capacitor/core': '6.2.2',
            '@capacitor/android': '6.2.2',
            '@capacitor/app': '6.0.3',
            '@capacitor/push-notifications': '6.0.3',
            '@capacitor/cli': '6.2.2'
          };

          const all = {
            ...(pkg.dependencies || {}),
            ...(pkg.devDependencies || {})
          };

          for (const [name, version] of Object.entries(expected)) {
            if (all[name] !== version) {
              console.error(
                `ERROR: ${name} expected ${version}, found ${all[name] || 'missing'}`
              );
              process.exit(1);
            }

            console.log(`PASS: ${name} ${version}`);
          }
          NODE

      # ============================================================
      # 9. VERIFY CAPACITOR CONFIG
      # ============================================================
      - name: Verify Capacitor configuration
        shell: bash
        run: |
          set -euo pipefail

          cd source

          node <<'NODE'
          const fs = require('fs');

          const text = fs.readFileSync(
            'capacitor.config.json',
            'utf8'
          );

          if (!text.includes('org.rkms.connect')) {
            console.error(
              'ERROR: org.rkms.connect not found in capacitor.config.json'
            );
            process.exit(1);
          }

          console.log('Capacitor APP ID: PASS');
          NODE

      # ============================================================
      # 10. VERIFY FIREBASE
      # ============================================================
      - name: Verify Firebase configuration
        shell: bash
        run: |
          set -euo pipefail

          cd source

          test -s google-services.json

          grep -q \
            '"package_name"[[:space:]]*:[[:space:]]*"org.rkms.connect"' \
            google-services.json

          echo "Firebase package: PASS"

      # ============================================================
      # 11. VERIFY FCM
      # ============================================================
      - name: Verify FCM pipeline
        shell: bash
        run: |
          set -euo pipefail

          cd source

          if [ -f scripts/verify-fcm-pipeline.py ]; then
            python3 scripts/verify-fcm-pipeline.py
          fi

      # ============================================================
      # 12. VERIFY SAVED LOGIN
      # ============================================================
      - name: Verify saved login
        shell: bash
        run: |
          set -euo pipefail

          cd source

          if [ -f scripts/verify-saved-login.py ]; then
            python3 scripts/verify-saved-login.py
          fi

      # ============================================================
      # 13. VERIFY NATIVE BRIDGE
      # ============================================================
      - name: Verify native bridge
        shell: bash
        run: |
          set -euo pipefail

          cd source

          if [ -f scripts/verify-native-bridge.py ]; then
            python3 scripts/verify-native-bridge.py
          fi

      # ============================================================
      # 14. VERIFY RKMS FINAL
      # ============================================================
      - name: Verify RKMS final
        shell: bash
        run: |
          set -euo pipefail

          cd source

          if [ -f scripts/verify-rkms-final.py ]; then
            python3 scripts/verify-rkms-final.py
          fi

      # ============================================================
      # 15. VERIFY CHAT MICROPHONE
      # ============================================================
      - name: Verify chat microphone
        shell: bash
        run: |
          set -euo pipefail

          cd source

          if [ -f scripts/verify-chat-mic-final.py ]; then
            python3 scripts/verify-chat-mic-final.py
          fi

      # ============================================================
      # 16. VERIFY FINAL THREE POINT FIX
      # ============================================================
      - name: Verify final three point fix
        shell: bash
        run: |
          set -euo pipefail

          cd source

          if [ -f scripts/verify-final-three-point-fix.py ]; then
            python3 scripts/verify-final-three-point-fix.py
          fi

      # ============================================================
      # 17. VERIFY ANDROID 16
      # ============================================================
      - name: Verify Android 16
        shell: bash
        run: |
          set -euo pipefail

          cd source

          if [ -f scripts/verify-android-16.py ]; then
            python3 scripts/verify-android-16.py
          fi

      # ============================================================
      # 18. NPM
      # ============================================================
      - name: Install npm dependencies
        shell: bash
        run: |
          set -euo pipefail

          cd source
          npm ci --no-audit --no-fund

      # ============================================================
      # 19. PUSH NOTIFICATIONS
      # ============================================================
      - name: Verify installed push notifications
        shell: bash
        run: |
          set -euo pipefail

          cd source

          node -e "
            const p=require('./node_modules/@capacitor/push-notifications/package.json');
            if(p.version!=='6.0.3'){
              console.error('Wrong push-notifications version:',p.version);
              process.exit(1);
            }
            console.log('push-notifications:',p.version);
          "

      # ============================================================
      # 20. WEB BUILD
      # ============================================================
      - name: Build web application
        shell: bash
        run: |
          set -euo pipefail

          cd source

          npm run build

          test -d dist
          test -f dist/index.html

          echo "Web build: PASS"

      # ============================================================
      # 21. FRESH ANDROID
      # ============================================================
      - name: Create fresh Android project
        shell: bash
        run: |
          set -euo pipefail

          cd source

          rm -rf android

          npx cap add android

      # ============================================================
      # 22. ANDROID 16
      # ============================================================
      - name: Configure Android 16 API 36
        shell: bash
        run: |
          set -euo pipefail

          cd source

          chmod +x scripts/configure-android-16.sh

          ./scripts/configure-android-16.sh

      # ============================================================
      # 23. SDK 36
      # ============================================================
      - name: Enforce Android SDK 36
        shell: bash
        run: |
          set -euo pipefail

          cd source

          if [ -f android/variables.gradle ]; then

            sed -i \
              -E 's/compileSdkVersion[[:space:]]*=[[:space:]]*[0-9]+/compileSdkVersion = 36/' \
              android/variables.gradle || true

            sed -i \
              -E 's/targetSdkVersion[[:space:]]*=[[:space:]]*[0-9]+/targetSdkVersion = 36/' \
              android/variables.gradle || true

            sed -i \
              -E 's/minSdkVersion[[:space:]]*=[[:space:]]*[0-9]+/minSdkVersion = 23/' \
              android/variables.gradle || true

          fi

      # ============================================================
      # 24. CAPACITOR SYNC
      # ============================================================
      - name: Sync Capacitor
        shell: bash
        run: |
          set -euo pipefail

          cd source

          npx cap sync android

      # ============================================================
      # 25. NATIVE SETUP
      # ============================================================
      - name: Setup RKMS Android native integration
        shell: bash
        run: |
          set -euo pipefail

          cd source

          chmod +x scripts/setup-android-native.sh

          ./scripts/setup-android-native.sh

      # ============================================================
      # 26. GOOGLE SERVICES FIX
      # ============================================================
      - name: Fix Google Services plugin version
        shell: bash
        run: |
          set -euo pipefail

          cd source

          test -f android/build.gradle
          test -f android/app/build.gradle

          sed -i \
            's/com\.google\.gms:google-services:4\.4\.0/com.google.gms:google-services:4.4.3/g' \
            android/build.gradle

          if ! grep -q \
            "com.google.gms.google-services" \
            android/app/build.gradle; then

            printf '\napply plugin: "com.google.gms.google-services"\n' \
              >> android/app/build.gradle
          fi

      # ============================================================
      # 27. VERIFY GOOGLE SERVICES
      # ============================================================
      - name: Verify Google Services plugin
        shell: bash
        run: |
          set -euo pipefail

          cd source

          grep -q \
            "com.google.gms:google-services:4.4.3" \
            android/build.gradle

          grep -q \
            "com.google.gms.google-services" \
            android/app/build.gradle

          echo "Google Services: PASS"

      # ============================================================
      # 28. VERIFY FIREBASE NATIVE
      # ============================================================
      - name: Verify Firebase native configuration
        shell: bash
        run: |
          set -euo pipefail

          cd source

          test -f android/app/google-services.json

          grep -q \
            '"package_name"[[:space:]]*:[[:space:]]*"org.rkms.connect"' \
            android/app/google-services.json

          echo "Firebase native configuration: PASS"

      # ============================================================
      # 29. VERIFY MAIN ACTIVITY
      # ============================================================
      - name: Verify MainActivity
        shell: bash
        run: |
          set -euo pipefail

          cd source

          MAIN_ACTIVITY="$(
            find android/app/src/main \
              -type f \
              -name MainActivity.java \
              -print -quit || true
          )"

          test -n "$MAIN_ACTIVITY"

          grep -q "RKMSNativeBridge" "$MAIN_ACTIVITY"
          grep -q "FirebaseApp.initializeApp" "$MAIN_ACTIVITY"
          grep -q "onNewIntent" "$MAIN_ACTIVITY"

          echo "MainActivity: PASS"

      # ============================================================
      # 30. FIREBASE MESSAGING
      # ============================================================
      - name: Ensure Firebase Messaging dependency
        shell: bash
        run: |
          set -euo pipefail

          cd source

          APP_GRADLE="android/app/build.gradle"

          if ! grep -q "firebase-messaging" "$APP_GRADLE"; then

            python3 - <<'PY'
          from pathlib import Path

          p = Path("android/app/build.gradle")
          text = p.read_text()

          dependency = """
              implementation 'com.google.firebase:firebase-messaging:25.0.1'
          """

          if "dependencies {" not in text:
              raise SystemExit("dependencies block not found")

          text = text.replace(
              "dependencies {",
              "dependencies {" + dependency,
              1
          )

          p.write_text(text)
          PY

          fi

          grep -q "firebase-messaging" "$APP_GRADLE"

      # ============================================================
      # 31. FINAL ANDROID CONFIG
      # ============================================================
      - name: Verify final Android configuration
        shell: bash
        run: |
          set -euo pipefail

          cd source

          test -f android/gradlew

          chmod +x android/gradlew

          grep -n "distributionUrl" \
            android/gradle/wrapper/gradle-wrapper.properties

          grep -RniE \
            "compileSdk|targetSdk|minSdk" \
            android \
            --include="*.gradle" \
            --include="*.gradle.kts" \
            | head -40 || true

          echo "Android configuration: PASS"

      # ============================================================
      # 32. GRADLE DEPENDENCIES
      # ============================================================
      - name: Resolve Gradle dependencies
        shell: bash
        run: |
          set -euo pipefail

          cd source/android

          ./gradlew \
            --no-daemon \
            --stacktrace \
            dependencies

      # ============================================================
      # 33. STATIC AUDIT
      # ============================================================
      - name: Static Android audit
        shell: bash
        run: |
          set -euo pipefail

          cd source

          MAIN_ACTIVITY="$(
            find android/app/src/main \
              -type f \
              -name MainActivity.java \
              -print -quit
          )"

          test -n "$MAIN_ACTIVITY"

          grep -q "FirebaseApp.initializeApp" "$MAIN_ACTIVITY"
          grep -q "RKMSNativeBridge" "$MAIN_ACTIVITY"
          grep -q "onNewIntent" "$MAIN_ACTIVITY"

          grep -Rqs "POST_NOTIFICATIONS" \
            android/app/src/main

          grep -Rqs "RECORD_AUDIO" \
            android/app/src/main

          echo "Static Android audit: PASS"

      # ============================================================
      # 34. SIGNING SECRETS
      # ============================================================
      - name: Verify release signing secrets
        shell: bash
        env:
          RKMS_KEYSTORE_BASE64: ${{ secrets.RKMS_KEYSTORE_BASE64 }}
          RKMS_KEYSTORE_PASSWORD: ${{ secrets.RKMS_KEYSTORE_PASSWORD }}
          RKMS_KEY_ALIAS: ${{ secrets.RKMS_KEY_ALIAS }}
          RKMS_KEY_PASSWORD: ${{ secrets.RKMS_KEY_PASSWORD }}
        run: |
          set -euo pipefail

          test -n "$RKMS_KEYSTORE_BASE64"
          test -n "$RKMS_KEYSTORE_PASSWORD"
          test -n "$RKMS_KEY_ALIAS"
          test -n "$RKMS_KEY_PASSWORD"

          echo "Release signing secrets: PASS"

      # ============================================================
      # 35. CONFIGURE RELEASE SIGNING
      # ============================================================
      - name: Configure release signing
        shell: bash
        env:
          RKMS_KEYSTORE_BASE64: ${{ secrets.RKMS_KEYSTORE_BASE64 }}
          RKMS_KEYSTORE_PASSWORD: ${{ secrets.RKMS_KEYSTORE_PASSWORD }}
          RKMS_KEY_ALIAS: ${{ secrets.RKMS_KEY_ALIAS }}
          RKMS_KEY_PASSWORD: ${{ secrets.RKMS_KEY_PASSWORD }}
        run: |
          set -euo pipefail

          cd source

          chmod +x scripts/configure-release-signing.sh

          ./scripts/configure-release-signing.sh

      # ============================================================
      # 36. FIX RELEASE SIGNING
      #
      # IMPORTANT:
      # Do NOT use System.getenv("RKMS_KEYSTORE_ABS") inside
      # build.gradle for storeFile.
      #
      # The previous run failed because that variable became null
      # in the next Gradle process.
      #
      # We now write the actual absolute keystore path directly
      # into build.gradle.
      # ============================================================
      - name: Fix release signing configuration
        shell: bash
        env:
          RKMS_KEYSTORE_PASSWORD: ${{ secrets.RKMS_KEYSTORE_PASSWORD }}
          RKMS_KEY_ALIAS: ${{ secrets.RKMS_KEY_ALIAS }}
          RKMS_KEY_PASSWORD: ${{ secrets.RKMS_KEY_PASSWORD }}
        run: |
          set -euo pipefail

          cd source

          APP_GRADLE="android/app/build.gradle"

          test -f "$APP_GRADLE"

          KEYSTORE="$(
            find android \
              -type f \
              \( -name "*.jks" -o -name "*.keystore" \) \
              -print -quit || true
          )"

          if [ -z "$KEYSTORE" ]; then
            echo "ERROR: Release keystore not found."
            exit 1
          fi

          KEYSTORE_ABS="$(cd "$(dirname "$KEYSTORE")" && pwd)/$(basename "$KEYSTORE")"

          echo "Release keystore located."
          echo "Path:"
          echo "$KEYSTORE_ABS"

          export RKMS_KEYSTORE_ABS="$KEYSTORE_ABS"

          python3 - "$KEYSTORE_ABS" <<'PY'
          from pathlib import Path
          import sys

          p = Path("android/app/build.gradle")
          text = p.read_text()

          keystore = sys.argv[1]

          marker = "// RKMS FORCE RELEASE SIGNING CONFIGURATION"

          if marker in text:
              text = text.split(marker, 1)[0].rstrip() + "\n"

          # Escape for Groovy single-quoted string.
          keystore_escaped = (
              keystore
              .replace("\\", "\\\\")
              .replace("'", "\\'")
          )

          block = f"""
          // RKMS FORCE RELEASE SIGNING CONFIGURATION
          android {{
              signingConfigs {{
                  release {{
                      storeFile file('{keystore_escaped}')
                      storePassword System.getenv('RKMS_KEYSTORE_PASSWORD')
                      keyAlias System.getenv('RKMS_KEY_ALIAS')
                      keyPassword System.getenv('RKMS_KEY_PASSWORD')
                  }}
              }}

              buildTypes {{
                  release {{
                      signingConfig signingConfigs.release
                  }}
              }}
          }}
          """

          p.write_text(
              text.rstrip() +
              "\n\n" +
              block.strip() +
              "\n"
          )

          print("Release signing configuration written.")
          print("Absolute keystore path written into Gradle configuration.")
          PY

          echo
          echo "Release signing block:"
          grep -n \
            -A14 \
            -B2 \
            "RKMS FORCE RELEASE SIGNING CONFIGURATION" \
            "$APP_GRADLE"

          test -f "$KEYSTORE_ABS"

          test -n "$RKMS_KEYSTORE_PASSWORD"
          test -n "$RKMS_KEY_ALIAS"
          test -n "$RKMS_KEY_PASSWORD"

          echo
          echo "Release signing configuration: PASS"

      # ============================================================
      # 37. VALIDATE KEYSTORE
      # ============================================================
      - name: Validate release keystore
        shell: bash
        env:
          RKMS_KEYSTORE_PASSWORD: ${{ secrets.RKMS_KEYSTORE_PASSWORD }}
        run: |
          set -euo pipefail

          cd source

          KEYSTORE="$(
            find android \
              -type f \
              \( -name "*.jks" -o -name "*.keystore" \) \
              -print -quit || true
          )"

          test -n "$KEYSTORE"

          echo "Keystore found."

          keytool \
            -list \
            -keystore "$KEYSTORE" \
            -storepass "$RKMS_KEYSTORE_PASSWORD" \
            >/dev/null

          echo "Keystore validation: PASS"

      # ============================================================
      # 38. GRADLE VERSION
      # ============================================================
      - name: Verify Gradle version
        shell: bash
        run: |
          set -euo pipefail

          cd source/android

          ./gradlew --version

      # ============================================================
      # 39. FIX MAINACTIVITY JAVA
      # ============================================================
      - name: Fix MainActivity Java syntax
        shell: bash
        run: |
          set -euo pipefail

          cd source

          MAIN_ACTIVITY="$(
            find android/app/src/main \
              -type f \
              -name MainActivity.java \
              -print -quit || true
          )"

          test -n "$MAIN_ACTIVITY"

          sed -i \
            's/""NOT_READY""/"NOT_READY"/g' \
            "$MAIN_ACTIVITY"

          sed -i \
            's/""ERROR""/"ERROR"/g' \
            "$MAIN_ACTIVITY"

          if grep -q \
            '""NOT_READY""\|""ERROR""' \
            "$MAIN_ACTIVITY"; then
            echo "ERROR: Malformed Java quotes still present."
            exit 1
          fi

          echo "MainActivity Java syntax fix: PASS"

      # ============================================================
      # 40. VALIDATE RELEASE CONFIGURATION
      # ============================================================
      - name: Validate release configuration
        shell: bash
        env:
          RKMS_KEYSTORE_PASSWORD: ${{ secrets.RKMS_KEYSTORE_PASSWORD }}
          RKMS_KEY_ALIAS: ${{ secrets.RKMS_KEY_ALIAS }}
          RKMS_KEY_PASSWORD: ${{ secrets.RKMS_KEY_PASSWORD }}
        run: |
          set -euo pipefail

          cd source/android

          ./gradlew \
            --no-daemon \
            --stacktrace \
            :app:compileReleaseJavaWithJavac \
            :app:processReleaseMainManifest

      # ============================================================
      # 41. CLEAN
      # ============================================================
      - name: Clean release build
        shell: bash
        run: |
          set -euo pipefail

          cd source/android

          ./gradlew \
            --no-daemon \
            clean

      # ============================================================
      # 42. BUILD ONLY SIGNED RELEASE APK
      # ============================================================
      - name: Build Signed Release APK
        shell: bash
        env:
          RKMS_KEYSTORE_PASSWORD: ${{ secrets.RKMS_KEYSTORE_PASSWORD }}
          RKMS_KEY_ALIAS: ${{ secrets.RKMS_KEY_ALIAS }}
          RKMS_KEY_PASSWORD: ${{ secrets.RKMS_KEY_PASSWORD }}
        run: |
          set -euo pipefail

          cd source/android

          ./gradlew \
            --no-daemon \
            --stacktrace \
            :app:assembleRelease

      # ============================================================
      # 43. VERIFY RELEASE APK
      # ============================================================
      - name: Verify release APK output
        shell: bash
        run: |
          set -euo pipefail

          cd source

          APK="$(
            find android/app/build/outputs/apk/release \
              -type f \
              -name "*.apk" \
              -print -quit || true
          )"

          if [ -z "$APK" ]; then
            echo "ERROR: Release APK not generated."
            exit 1
          fi

          echo "Release APK:"
          ls -lh "$APK"

          case "$APK" in
            *debug*)
              echo "ERROR: Debug APK detected."
              exit 1
              ;;
          esac

          echo "Release APK output: PASS"

      # ============================================================
      # 44. VERIFY NO AAB
      # ============================================================
      - name: Verify no AAB is required
        shell: bash
        run: |
          set -euo pipefail

          cd source

          echo "APK-only workflow."

          if find android/app/build/outputs \
            -type f \
            -name "*.aab" \
            | grep -q .; then

            echo "ERROR: AAB generated."
            exit 1
          fi

          echo "APK-only output: PASS"

      # ============================================================
      # 45. VERIFY PACKAGE
      # ============================================================
      - name: Verify APK package name
        shell: bash
        run: |
          set -euo pipefail

          cd source

          APK="$(
            find android/app/build/outputs/apk/release \
              -type f \
              -name "*.apk" \
              -print -quit
          )"

          AAPT="$ANDROID_SDK_ROOT/build-tools/36.0.0/aapt"

          PACKAGE_NAME="$(
            "$AAPT" dump badging "$APK" \
              | sed -n "s/^package: name='\([^']*\)'.*/\1/p" \
              | head -1
          )"

          echo "APK package: $PACKAGE_NAME"

          test "$PACKAGE_NAME" = "org.rkms.connect"

          echo "Package name: PASS"

      # ============================================================
      # 46. VERIFY SIGNATURE
      # ============================================================
      - name: Verify APK signature
        shell: bash
        run: |
          set -euo pipefail

          cd source

          APK="$(
            find android/app/build/outputs/apk/release \
              -type f \
              -name "*.apk" \
              -print -quit
          )"

          APKSIGNER="$ANDROID_SDK_ROOT/build-tools/36.0.0/apksigner"

          "$APKSIGNER" verify \
            --verbose \
            "$APK"

          echo "APK signature: PASS"

      # ============================================================
      # 47. SHA256
      # ============================================================
      - name: Generate APK SHA256
        shell: bash
        run: |
          set -euo pipefail

          cd source

          APK="$(
            find android/app/build/outputs/apk/release \
              -type f \
              -name "*.apk" \
              -print -quit
          )"

          sha256sum "$APK"

      # ============================================================
      # 48. PREPARE FINAL APK
      # ============================================================
      - name: Prepare final APK
        shell: bash
        run: |
          set -euo pipefail

          cd source

          APK="$(
            find android/app/build/outputs/apk/release \
              -type f \
              -name "*.apk" \
              -print -quit
          )"

          cp "$APK" \
            ../RKMS_CONNECT_FINAL_SIGNED_RELEASE.apk

          sha256sum \
            ../RKMS_CONNECT_FINAL_SIGNED_RELEASE.apk \
            > ../RKMS_CONNECT_FINAL_SIGNED_RELEASE.apk.sha256

          ls -lh \
            ../RKMS_CONNECT_FINAL_SIGNED_RELEASE.apk \
            ../RKMS_CONNECT_FINAL_SIGNED_RELEASE.apk.sha256

      # ============================================================
      # 49. UPLOAD ONLY FINAL SIGNED RELEASE APK
      # ============================================================
      - name: Upload final signed release APK
        uses: actions/upload-artifact@v4
        with:
          name: RKMS-CONNECT-FINAL-SIGNED-RELEASE-APK
          if-no-files-found: error
          path: |
            RKMS_CONNECT_FINAL_SIGNED_RELEASE.apk
            RKMS_CONNECT_FINAL_SIGNED_RELEASE.apk.sha256
